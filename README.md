# Reasongine Observatory

Public results snapshot with hypotheses, aggregate metrics and costs.
The root-page curated traces include tool activity and final written analyses, without raw tool payloads.
Hypothesis text may describe observed biological effects. Historical ordering limitations are marked in each run.

The [best-hypothesis execution record](https://parsaidp.github.io/reasongine-observatory/hypotheses/best-trace/)
adds all 26 recorded generator/predictor tool actions, explicitly reviewed commands
and selected outputs, saved prompts, final reports, and a generated prediction
script as text. No subagents were invoked in that run. Private thinking,
unreviewed outputs, raw input files, per-feature vectors, credentials and private
repository source remain excluded. This is a reviewed record, not a raw transcript.
